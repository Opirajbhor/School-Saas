

  // ------- tanstack Query  -------------
const { data: subjects = [], isPending } = useQuery({
    queryKey: ["subjects"],
    queryFn: async () => {
      const result = await getSubjects();
      if (!result.success) {
        throw new Error(result.error);
      }
      return result.data as SubjectType[];
    },
  });



  --------------- for update state -----------
  const queryClient = useQueryClient();

---- inside the button ---------
  queryClient.invalidateQueries({ queryKey: ["classTeacher", "assigned"] });