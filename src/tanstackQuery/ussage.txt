

  // ------- tanstack Query  -------------
  const { data: classInfo = [], isPending } = useQuery({
    queryKey: ["activeClassSection"],
    queryFn: async () => fetchData(getActiveClassesSection),
  });



  --------------- for update state -----------
  const queryClient = useQueryClient();

---- inside the button ---------
  queryClient.invalidateQueries({ queryKey: ["classTeacher", "assigned"] });